apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ${HOST_NAME}-cert
  namespace: ${HOST_NAME}
spec:
  secretName: ${HOST_NAME}-tls
  dnsNames:
    - ${HOST_NAME}.local.ruizops.com
  issuerRef:
    name: letsencrypt-dns
    kind: ClusterIssuer
