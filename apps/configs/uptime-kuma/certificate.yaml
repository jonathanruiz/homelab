apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: uptime-kuma-cert
  namespace: uptime-kuma
spec:
  secretName: uptime-kuma-tls
  dnsNames:
    - uptime.local.ruizops.com
  issuerRef:
    name: letsencrypt-dns
    kind: ClusterIssuer
