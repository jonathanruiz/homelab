apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: it-tools-cert
  namespace: it-tools
spec:
  secretName: it-tools-tls
  dnsNames:
    - it-tools.local.ruizops.com
  issuerRef:
    name: letsencrypt-dns
    kind: ClusterIssuer
