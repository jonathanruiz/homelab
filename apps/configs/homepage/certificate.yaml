apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: homepage-cert
  namespace: homepage
spec:
  secretName: homepage-tls
  dnsNames:
    - homepage.local.ruizops.com
  issuerRef:
    name: letsencrypt-dns
    kind: ClusterIssuer
