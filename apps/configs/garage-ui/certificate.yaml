apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: garage-ui-cert
  namespace: garage-ui
spec:
  secretName: garage-ui-tls
  dnsNames:
    - garage-ui.local.ruizops.com
  issuerRef:
    name: letsencrypt-dns
    kind: ClusterIssuer
