apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: forgejo-cert
  namespace: forgejo
spec:
  secretName: forgejo-tls
  dnsNames:
    - forgejo.local.ruizops.com
  issuerRef:
    name: letsencrypt-dns
    kind: ClusterIssuer
