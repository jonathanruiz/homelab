apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: checkmate-cert
  namespace: checkmate
spec:
  secretName: checkmate-tls
  dnsNames:
    - checkmate.local.ruizops.com
    - checkmate-api.local.ruizops.com
  issuerRef:
    name: letsencrypt-dns
    kind: ClusterIssuer
