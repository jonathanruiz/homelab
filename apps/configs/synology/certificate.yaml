apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: synology-cert
  namespace: synology
spec:
  secretName: synology-tls
  dnsNames:
    - synology.local.ruizops.com
  issuerRef:
    name: letsencrypt-dns
    kind: ClusterIssuer
