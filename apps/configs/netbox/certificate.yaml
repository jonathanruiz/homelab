apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: netbox-cert
  namespace: netbox
spec:
  secretName: netbox-tls
  dnsNames:
    - netbox.local.ruizops.com
  issuerRef:
    name: letsencrypt-dns
    kind: ClusterIssuer
