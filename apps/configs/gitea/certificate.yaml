apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gitea-cert
  namespace: gitea
spec:
  secretName: gitea-tls
  dnsNames:
    - gitea.local.ruizops.com
  issuerRef:
    name: letsencrypt-dns
    kind: ClusterIssuer
