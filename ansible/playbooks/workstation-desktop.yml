---
- name: Configure workstation XFCE desktop
  hosts: workstations
  become: true

  tasks:
    - name: Install XFCE and XRDP packages
      ansible.builtin.apt:
        name:
          - xfce4
          - xfce4-goodies
          - xrdp
          - firefox-esr
          - dbus-x11
        state: present
        update_cache: true

    - name: Configure XFCE session for XRDP
      ansible.builtin.copy:
        dest: "/home/{{ ansible_user }}/.xsession"
        content: "startxfce4\n"
        owner: "{{ ansible_user }}"
        group: "{{ ansible_user }}"
        mode: "0755"

    - name: Add xrdp user to ssl-cert group
      ansible.builtin.user:
        name: xrdp
        groups: ssl-cert
        append: true

    - name: Enable and start xrdp
      ansible.builtin.systemd:
        name: xrdp
        enabled: true
        state: started
