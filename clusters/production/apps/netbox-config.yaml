apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: netbox-config
  namespace: flux-system
spec:
  interval: 10m
  path: ./apps/configs/netbox
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: netbox-controller
