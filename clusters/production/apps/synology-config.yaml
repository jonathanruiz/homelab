apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: synology-config
  namespace: flux-system
spec:
  interval: 10m
  path: ./apps/configs/synology
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: synology-controller
