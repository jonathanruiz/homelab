apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: garage-config
  namespace: flux-system
spec:
  interval: 10m
  path: ./infrastructure/configs/garage
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: garage-controllers
