apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: traefik-crds
  namespace: traefik
spec:
  interval: 10m
  chart:
    spec:
      chart: traefik-crds
      version: 35.0.1
      sourceRef:
        kind: HelmRepository
        name: traefik
        namespace: traefik
