apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: garage-cert
  namespace: garage
spec:
  secretName: garage-tls
  dnsNames:
    - garage.local.ruizops.com
  issuerRef:
    name: letsencrypt-dns
    kind: ClusterIssuer
